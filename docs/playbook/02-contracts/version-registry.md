# Version Registry

Track versions for strategies, prompts, models, datasets, calculations, indicators, risk models, validation configurations, evidence schemas, APIs, contracts, agent definitions, and execution adapters.

Immutable versioning is mandatory for reproducibility. Do not silently change a strategy while preserving its historical performance statistics.
