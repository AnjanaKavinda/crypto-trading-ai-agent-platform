# Acceptance Criteria

A feature is complete only when contracts, implementation, tests, audit/provenance, docs, security boundaries, and failure behavior are complete.

A trading feature is not complete unless it proves: no approval = no execution, risk controls cannot be bypassed, execution intent requires valid approval, and all decisions are reconstructable.
