# Agent Handoff Workflow
Architect -> defines boundaries/contracts/dependencies/acceptance criteria.
Implementation Agent -> implements one issue and tests it.
QA/Security -> independently reviews/tests and reports blockers.
Human -> accepts/rejects/merges.
Architect -> updates architecture/traceability if material.

Parallel work is allowed only when authoritative contracts/boundaries do not overlap.
