# Secrets Policy
Never put real secrets in repository files, prompts, LLM context, tests, fixtures, docs, issues, PRs or logs. Use least-privilege secret management. Separate read/analysis credentials from execution credentials. AI agents should not receive unrestricted exchange credentials.
