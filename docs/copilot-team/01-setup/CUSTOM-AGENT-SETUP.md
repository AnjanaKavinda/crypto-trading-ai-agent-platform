# Custom Agent Setup
Repository root must contain:
`.github/agents/architect.agent.md`, `backend-foundation.agent.md`, `trading-intelligence.agent.md`, `qa-security.agent.md`.

Do not confuse these with `.github/instructions/*.instructions.md` (path rules) or `docs/playbook/03-agents` (runtime trading-agent architecture).
